세션관리

--continue: 방금 끝낸 세션을 줄러줌
--resume : 현재 까지 히스토리 알려줌
/renate : resume 시 내가 식별할수 있게 세션을 명명

트리거 키워드 설정해줘 
라고 하면 반복적인 업무를 트리거로 자동화 할 수 있다.


"지금까지 작업 내용을 HANDOFF.md로 정리해주는 hook을 만들어줘. 훅은 /handoff command로 실행되게 해줘. 해당 훅은 다음 에이전트가 이 파일만 읽고 이어갈 수 있게 시도한 것, 성공한 것, 실패한 것, 다음 단계 적어주면 되. 범위는 전역 설정으로 해줘."



#git 관련	
## 커맨드
### 리눅스 커맨드
ls  : 폴더내 뭐가 있는지
ls -al : 숨긴것 까지
touch : 파일 만드는 리눅스 용어

### git 커맨드	
* **init : git** 
사용 폴더로 세팅(그전에 우클릭 bash로 실행 필요)
* **git config user.name**
사용자 이름 출력
* **working Directory**
 work space를 뜻함(아직 깃을 안했을 수도 있음)
* **staging area**
수정이력 대기 장소
* **Repository**
staging 중 파일이 수정이력 관리를 위해 최종 기록되는 장소
* **git status **
현재 상황 보기

git add [파일명]: working directory 내 문서를 스테이징 영역에 추가
git add . : '.'을 붙이면 Working Directory 내 있는 모든 언트랙티드 사항을 올린다.

git commit : 스테이징 -> 리포지토리
git commit -m "{이곳에 내용기재}"  : 에디터를 거치지 않고 한 번에 처리할 수 있습니다.

git log - 커밋수정이력확인



commit 후에는 화면이 바뀌는 데 
shift+i 누르고
(insert 모드)
수정이력 관련 코멘트를 남김
esc 누르면 insert 모드 없어짐
shift+;
wq (엔터)


## gitignore.
touch .gitignore : gitignore 폴더를 만드는 법. 
nano .gitignore : nano라는 vim 인터페이스가 생김


--

## **git log 옵션 & 커밋 이동 명령어**

* **`$ git log -p**
    커밋의 변경사항까지 함께 출력하는 옵션
* **`$ git log -{#}**
   숫자에 맞는 로그만 보여줌.
* ** (예시) $git log -p -1 
   직전 한 개의 로그에 변경 사항까지 자세히 cnffur.
* **`$ git log --oneline`**
    각 커밋을 요약해 한 줄씩 출력하는 옵션
* **`$ git checkout 커밋해시`**
    문서 내용을 특정 커밋의 내용으로 되돌리는 깃 명령어
* **`$ git reflog`**
    HEAD 포인터의 참조 이력을 출력하는 깃 명령어

*** q : 로그에서 나가고 싶을 때에는 큐를 누른다. 

--

## git 실수 대응
* ** git reset 파일명**
1) 커밋할 생각이 없는데, 스테이징해버렸을때 워킹스테이지로 옮김
- 그냥 git reset으로 가능
2) 이미 커밋했을경우
git reset 커밋해시
--hard, --mixed, --soft



### git reset 옵션별 비교

| 옵션 | 워킹 디렉터리 | 스테이징 영역 | 리포지토리 |
| :--- | :--- | :--- | :--- |
| **--soft** | 현 상태 유지 | 현 상태 유지 | 커밋 이동 |
| **--mixed** | 현 상태 유지 | 스테이징 초기화 | 커밋 이동 |
| **--hard** | 이동한 커밋의 상태로 변경 | 스테이징 초기화 | 커밋 이동 |


### git revert 커밋해시(실수한 커밋해시를 쓴다. 돌아가려는 커밋해시가 아니라)
수정사항을 돌리면서 이조차 기록으로 남김



## Git 브랜치 관련 주요 명령어
$ git branch
현재 브랜치 목록을 볼 수 있는 깃 명령어

$ git branch 브랜치이름
새로운 브랜치를 생성하는 깃 명령어

$ git checkout 브랜치이름
작업 중인 브랜치를 변경하는 깃 명령어

$ git merge 브랜치이름
현재 브랜치에 다른 브랜치의 내용을 병합하는 깃 명령어

$ git branch -d 브랜치이름
해당 브랜치를 삭제함



## **Git원격 저장소 이용 관련 명령어들**

* **`$ git remote -v`**
    현재 깃 프로젝트에 등록된 원격 저장소 확인하는 깃 명령어

* **`$ git remote add 원격저장소이름 원격저장소주소`**
    현재 깃 프로젝트에 원격 저장소를 등록하고, 여기에 이름(별칭)을 붙이는 깃 명령어

* **`$ git push`**
    로컬 저장소의 내용을 원격 저장소에 공유할 때 사용하는 깃 명령어
   그러나 로컬 저장소랑 원격저장소의 프렌치 내용이 다를 수 있기 때문에 업스트림 셋을 해야 된다
 
* **`$ git push -u origin master`**
원격저장소의 브랜치 명까지 써줘야함

* **`$ git clone 원격저장소이름`**
원격저장소에 있는 내용을 그대로 복사해서 가지고 온다

* **`$ git pull`**
    원격 저장소의 내용을 로컬 저장소로 가져와 자동 병합하는 깃 명령어

* **'$ git remote add origin 원격저장소주소**
(origin 부분은 이름이라 다른 명칭을 써도 되나 보통 원본저장소를 origin으로 쓰는 관례가 있음)

* ** fetch / push**
패치는 내려받을 때 쓰는 거고 표시는 내 내용을 올릴 때 쓰는 거야